# Supabase credentials for persistent cache (Streamlit Community Cloud, etc.)
# Get these from your Supabase project: Dashboard → Settings → API
#
# Create a Storage bucket (default: fighter-cache) in Storage → New bucket.
# Use the service role key for full read/write.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key

# Optional: custom bucket name (default: fighter-cache)
# SUPABASE_CACHE_BUCKET=fighter-cache

# GA4 measurement ID (required for analytics; set in Railway Variables for prod)
# GA4_MEASUREMENT_ID=G-5R0QD7V4X0

# Alternative: Supabase docs use SUPABASE_SERVICE_ROLE_KEY
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
