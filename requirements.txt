# Railway / deployment – mirrors pyproject.toml deps
pandas>=2.0
requests>=2.28
plotly>=5.0
streamlit>=1.28
supabase>=2.0
python-dotenv>=1.0
pillow>=10.0

# Install the project (src/ufc_stats, src/standard_metrics)
-e .
